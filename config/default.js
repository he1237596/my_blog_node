const config = {
  // 启动端口
  port: 3002,
  // 数据库配置
  database: {
    DATABASE: 'koa2_blog',
    USERNAME: 'root',
    PASSWORD: '123456',
    PORT: '3308',
    HOST: 'localhost'
  }
}

module.exports = config;